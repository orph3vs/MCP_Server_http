# Code Review

## 검토 기준
- 초기 설계 문서: `docs/nlic-mcp-architecture.md`
- 현재 파이프라인 문서: `docs/request-pipeline.md`
- 운영/변경 메모: `dev_log.md`
- 이번 대화에서 반복해서 확인한 요구사항:
  - `stdio`/`http`는 같은 코어를 공유해야 함
  - 특정 법령 하드코딩이 아니라 범용적으로 적용되어야 함
  - 원칙 조항뿐 아니라 직접 허용/예외 조항까지 찾아야 함
  - 가능하면 `조`뿐 아니라 `항/호`까지 근거를 제시해야 함

## 총평
현재 구현은 `stdio`/`http` 공용 코어, 리스크 분류, 로그 적재, 조문/항/호 스캔, 공개 링크 정리까지 상당 부분 진척되었습니다.
하지만 검색/랭킹 계층에는 아직 특정 질문군(특히 학교밖청소년) 보정이 남아 있고, 예외조항 탐색이 `고유식별정보/민감정보` 질문군에 편중되어 있어 “범용 법령 해석 서버”라는 최초 목표와는 거리가 남아 있습니다.

즉 현재 상태는:
- transport/로그/출력 인프라는 꽤 정리됨
- retrieval/grounding 규칙은 아직 범용 엔진이라기보다 “일부 질문군에 강한 v1.5”에 가까움

## 주요 Findings

### 1. [높음] 케이스성 랭킹 보정이 코어에 남아 있어 범용성 요구와 충돌
- 파일: `src/request_pipeline.py:377-388`
- 파일: `src/request_pipeline.py:709-726`
- 내용:
  - `_sensitive_identifier_keywords()`에서 `학교밖청소년지원센터`, `가정 밖 청소년`, `통합정보시스템` 같은 특정 도메인 키워드를 직접 주입하고 있습니다.
  - `_related_law_relevance()`에서도 `학교밖청소년`, `성보호`, `지원센터`를 조건으로 가산/감산점이 들어갑니다.
- 문제:
  - 초기 설계의 `Query Normalizer`/`Retriever Agent`는 범용 규칙 기반이어야 하는데, 현재는 특정 질문군 성공을 위해 코어 스코어러에 사례성 보정이 들어가 있습니다.
  - 다른 법령군(예: 노인일자리법, 의료법, 고용보험법)으로 갈수록 예외조항 탐색 품질이 불안정해질 수밖에 없습니다.
- 보완 방향:
  - 법명/질문 토큰/관련법 관계코드 기반의 일반 점수 체계로 정리
  - 특정 기관명/법명 보정은 코어가 아니라 외부 설정 또는 suggestion layer로 분리

### 2. [높음] 직접 허용/예외 조항 탐색이 `민감정보·고유식별정보` 질문군으로 한정됨
- 파일: `src/request_pipeline.py:373-376`
- 파일: `src/request_pipeline.py:901-956`
- 내용:
  - `_find_keyword_matched_article()`는 `_is_sensitive_identifier_question()`이 참일 때만 동작합니다.
  - 즉 현재의 “키워드로 조문 전체를 스캔해서 직접 허용 조항을 찾는 기능”은 사실상 주민등록번호/고유식별정보/민감정보 질문군에만 열려 있습니다.
- 문제:
  - 최초 설계의 핵심은 “법령 원문 기반 답변”과 “근거가 없는 문장 금지”인데, 예외조항/특례조항/적용제외 조항을 찾는 능력이 특정 질문군으로 제한되면 범용 법령 해석 품질이 낮아집니다.
  - 예: 특례, 적용제외, 예외, 허용, 금지, 단서, 불가피, 위탁, 정보시스템 등은 개인정보 질문 외에도 광범위하게 중요합니다.
- 보완 방향:
  - 질문군별 하드 분기 대신 “조문 제목/문언 패턴 기반의 일반 clause scan” 계층 추가
  - 예외/특례/허용/적용제외/단서 조문을 전반적으로 탐색할 수 있는 공통 정책 필요

### 3. [중간] 검색 흐름이 첫 성공 검색어에 너무 빨리 고정되어 초기 앵커 편향이 남아 있음
- 파일: `src/request_pipeline.py:1322-1333`
- 내용:
  - `process()`에서 `search_queries`를 순회하다 첫 번째로 결과가 나온 시점에 `law_data`를 잡고 `break`합니다.
  - 이후 탐색도 있긴 하지만, 초기에 잡힌 법령 집합이 후속 단계의 출발점이 됩니다.
- 문제:
  - 질문이 애매하거나 법령명이 섞여 있는 경우, 가장 먼저 맞은 “넓은 법”이 대표 법령으로 굳어질 가능성이 높습니다.
  - 이후 관련법 확장과 조문 스캔이 있어도 초기 앵커가 잘못 잡히면 correction cost가 커집니다.
- 보완 방향:
  - “첫 성공”이 아니라 상위 N개 검색 결과를 모아 전역 랭킹 후 대표 법령 선택
  - 최소한 민감/허용 여부 질문에서는 초기 search hit를 즉시 확정하지 말고 1차 후보군을 합친 뒤 평가

### 4. [중간] 테스트 스위트가 현재 green 상태가 아니어서 회귀 방지 신뢰도가 떨어짐
- 파일: `tests/test_cost_logger.py:108-110`
- 증상:
  - 전체 테스트 실행 결과:
    - `python -m unittest discover -s tests -p 'test_*.py' -q`
    - `NameError: name 'os' is not defined`
- 문제:
  - 현재 회귀 방지 체계를 강조하고 있지만, 전체 테스트 스위트가 깨진 상태면 실제 변경 이후 안전망 역할을 못 합니다.
- 보완 방향:
  - `tests/test_cost_logger.py`에 `import os` 추가
  - CI/로컬 공통 기준으로 “discover 전체 통과”를 기본 확인 절차로 복구

### 5. [중간] 공통 코어가 `mcp_stdio_server.py`에 들어 있어 구조적 의도가 코드에 잘 드러나지 않음
- 파일: `src/mcp_stdio_server.py`
- 파일: `src/mcp_http_server.py`
- 내용:
  - 실제 공통 MCP 코어(`McpServer`)가 `src/mcp_stdio_server.py` 안에 있고, HTTP 서버가 이를 import해 씁니다.
- 문제:
  - 기능적으로는 동작하지만, 설계 문서의 “MCP Server API Layer / 공통 코어” 관점과 파일 구조가 어긋나 있어 새 작업 때 transport와 core를 혼동하기 쉽습니다.
  - 이번 작업에서 실제로 stdio/http를 별개 프로젝트처럼 다루게 된 원인 중 하나이기도 합니다.
- 보완 방향:
  - `src/mcp_core.py` 또는 `src/mcp_server_core.py` 같은 공통 모듈로 `McpServer` 분리
  - stdio/http는 transport entrypoint만 담당하게 명확화

## 설계와 잘 맞는 부분

### 1. 공통 코어 + 다중 transport 방향은 유지됨
- `src/mcp_stdio_server.py`의 `McpServer`
- `src/mcp_http_server.py`의 HTTP wrapper
- `RequestPipeline`, `AnswerComposer`, `NlicApiWrapper`는 transport 공용으로 사용됨

### 2. 조문뿐 아니라 `항/호/목`까지 파고드는 기반은 이미 있음
- `src/nlic_api_wrapper.py:407-530`
- `_best_match_in_unit()`와 `find_article_by_keywords()`는 `항/호/목` 단위 매칭을 지원
- 즉 문제는 “스캔 엔진 부재”보다 “언제/어떻게 이 엔진을 범용적으로 켤지”에 더 가깝습니다

### 3. 링크 정책, 로그, 인증, HTML 대시보드는 실사용 수준으로 많이 정리됨
- `OC` 비노출
- 공개 브라우저 링크 정리
- `MCP_AUTH_TOKEN`
- `8000` 로그/REST와 `8001` MCP 분리
- SQLite 로그 축적 및 대시보드 조회

## 우선순위별 보완 권장

### 1순위: 케이스성 규칙 제거 + 범용 clause scan 정책 재설계
- `학교밖청소년` 전용 점수 보정 제거
- 공통 규칙으로 아래 유형을 찾는 계층 도입:
  - 예외
  - 특례
  - 적용 제외
  - 처리할 수
  - 불가피
  - 위탁
  - 정보시스템
  - 단서
- 이 계층은 특정 법률명이 아니라 “질문의 법명 + 관련법령/시행령 + 조문 제목/문언 패턴”에 반응해야 함

### 2순위: 대표 법령 선택을 “첫 검색 성공”에서 “후보군 전역 평가”로 변경
- 현재는 첫 hit 편향이 남아 있음
- 법령군 전체를 모은 뒤:
  - 법령명 일치
  - 시행령/시행규칙 여부
  - 관련법령 네트워크
  - 직접 허용 조항 점수
  를 합쳐 대표 법령을 고르는 쪽이 설계 취지에 맞음

### 3순위: 회귀 테스트를 “사례별 하드코딩 검증”이 아니라 “규칙 검증”으로 재구성
- 특정 법 하나를 맞췄는지보다, 아래를 테스트해야 함:
  - 허용/예외형 질문에서 시행령 후보가 앞쪽으로 오르는지
  - 직접 허용 제목 조문이 일반 원칙 조문보다 높은 점수를 받는지
  - 관련법령 네트워크 확장 후 대표 법령이 재선정되는지
- 단, 회귀 샘플은 여러 도메인으로 분산해 두는 것이 좋음
  - 의료
  - 청소년
  - 노인복지/노인일자리
  - 고용
  - 교육

### 4순위: MCP 공통 코어 모듈 분리
- 기능 변화는 크지 않지만, 이후 작업 혼선을 줄이는 효과가 큼
- `McpServer`를 transport 파일 밖으로 빼면 stdio/http 동시 지원 구조가 더 선명해짐
- mcp_core 로 분리

## 결론
현재 구현은 “법령 원문 기반 MCP 서버”의 뼈대는 충분히 갖췄습니다.
하지만 retrieval 핵심부에는 아직 특정 사례를 성공시키기 위한 조정이 남아 있고, 이 때문에 범용 법령 해석 품질이 질문군별로 크게 흔들립니다.

가장 중요한 다음 단계는:
- 특정 법령 케이스를 더 붙이는 것이 아니라
- 예외/허용 조항을 범용적으로 찾는 규칙으로 코어를 다시 세우는 것
입니다.

이번 리뷰 기준으로는, 새 기능 추가보다 먼저 이 retrieval 일반화 작업을 우선하는 것이 맞습니다.
